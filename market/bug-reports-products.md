# Bug Reports — Products Module (QA Cloud Market API)

---

## BUG-001 — POST и PUT /api/groceries принимают отрицательное значение price

| Параметр | Значение |
|---|---|
| ID | BUG-001 |
| Severity | Major |
| Priority | High |
| Окружение | qacloud.dev Market API, Postman |
| Связанные тест-кейсы | PROD-018, PROD-025 |

### Шаги воспроизведения

**Создание товара:**
1. Отправить `POST /api/groceries` с телом:
   ```json
   { "name": "Test Product", "price": -10, "category": "Dairy" }
   ```

**Обновление товара:**
2. Отправить `PUT /api/groceries/:id` (id — валидный существующий товар) с телом:
   ```json
   { "price": -24 }
   ```

### Ожидаемый результат
`400 Bad Request` в обоих случаях — цена товара не может быть отрицательной.

### Фактический результат
- POST: `201 Created`, товар создаётся с `price: -10`
- PUT: `200 OK`, цена товара обновляется на `-24`

Подтверждено повторным `GET /api/groceries/:id` — отрицательное значение сохраняется в БД, а не является отображаемой ошибкой.


---

## BUG-002 — Несогласованная валидация price = 0 между POST (400) и PUT (200)

| Параметр | Значение |
|---|---|
| ID | BUG-002 |
| Severity | Major |
| Priority | Medium |
| Окружение | qacloud.dev Market API, Postman |
| Связанные тест-кейсы | PROD-017, PROD-024 |

### Шаги воспроизведения

**Создание товара:**
1. Отправить `POST /api/groceries` с телом:
   ```json
   { "name": "Test Product", "price": 0, "category": "Dairy" }
   ```

**Обновление товара:**
2. Отправить `PUT /api/groceries/:id` (id — валидный существующий товар) с телом:
   ```json
   { "price": 0 }
   ```

### Ожидаемый результат
Одинаковое поведение на обоих эндпоинтах: оба возвращают `400 Bad Request`.

### Фактический результат
- POST: `400 Bad Request`
- PUT: `200 OK`, цена товара обновляется на `0`

Один и тот же бизнес-инвариант ("цена не может быть нулевой") соблюдается при создании и не соблюдается при обновлении.


---

## BUG-003 — Сообщение об ошибке при price = 0 не соответствует причине ошибки

| Параметр | Значение |
|---|---|
| ID | BUG-003 |
| Severity | Minor |
| Priority | Low |
| Окружение | qacloud.dev Market API, Postman |
| Связанные тест-кейсы | PROD-017 |

### Шаги воспроизведения
1. Отправить `POST /api/groceries` с телом:
   ```json
   { "name": "Test Product", "price": 0, "category": "Dairy" }
   ```

### Ожидаемый результат
`400 Bad Request` с сообщением, корректно описывающим причину ошибки, а именно недопустимое значение `price`.

### Фактический результат
`400 Bad Request`, но текст ошибки:
```json
{ "error": "product_name/name, price, and category are required" }
```
Сообщение сформулировано так, будто поле `price` отсутствует в запросе, хотя оно присутствует.


---

## BUG-004 — Категория Meat, заявленная в документации, отсутствует в дефолтном каталоге

| Параметр | Значение |
|---|---|
| ID | BUG-004 |
| Severity | Major |
| Priority | Medium |
| Окружение | qacloud.dev Market API, Postman + Wiki документация |
| Связанные тест-кейсы | PROD-005, PROD-006 |

### Шаги воспроизведения
1. Выполнить POST /api/reset.
2. Выполнить GET /api/groceries и проверить категории.
3. Отправить GET /api/groceries/filter?category=Meat
4. Отправить GET /api/groceries/filter?category=meat-seafood

### Ожидаемый результат
Запрос по категории `Meat`, заявленной в документации как часть дефолтного набора сид-данных, должен возвращать соответствующие товары.

### Фактический результат
- `category=Meat` → `200 OK`, `[]` (пустой список)
- `category=meat-seafood` → `200 OK`, список с товарами этой категории

---

# Bug Reports — Basket Module (QA Cloud Market API)

---

## BUG-B01 — quantity = 0 возвращает сообщение "required" вместо "must be at least 1"

| Параметр | Значение |
|---|---|
| ID | BUG-B01 |
| Severity | Minor |
| Priority | Low |
| Окружение | qacloud.dev Market API, Postman |
| Связанные тест-кейсы | BASK-004 |

### Шаги воспроизведения

**Добавление товара в корзину:**
1. Отправить `POST /api/basket` с телом:
   ```json
   { "product_id": "<валидный id>", "quantity": 0 }
   ```

**Обновление количества в корзине:**
2. Отправить `PUT /api/basket/:id` (id — валидная существующая позиция корзины) с телом:
   ```json
   { "quantity": 0 }
   ```

### Ожидаемый результат
`400 Bad Request` с сообщением `"quantity must be at least 1"` в обоих случаях — по аналогии с поведением при отрицательном `quantity` (`-1`, `-2`), где именно это сообщение и возвращается.

### Фактический результат
`400 Bad Request` в обоих случаях, но текст ошибки:
```json
{ "error": "product_id and quantity are required" }
```
Сообщение сформулировано так, будто поле `quantity` отсутствует в запросе, хотя оно присутствует и равно `0`. При этом `quantity: -1`/`-2` в тех же условиях корректно возвращает `"quantity must be at least 1"`.

---

## BUG-B02 — Невалидный тип quantity/id вызывает 500 с сырой ошибкой БД вместо 400

| Параметр | Значение |
|---|---|
| ID | BUG-B02 |
| Severity | Major |
| Priority | High |
| Окружение | qacloud.dev Market API, Postman |
| Связанные тест-кейсы | BASK-005, BASK-008, BASK-010 |

### Шаги воспроизведения

**Добавление товара с нечисловым quantity:**
1. Отправить `POST /api/basket` с телом:
   ```json
   { "product_id": "<валидный id>", "quantity": "asf" }
   ```

**Обновление количества нечисловым значением:**
2. Отправить `PUT /api/basket/:id` (id — валидная существующая позиция корзины) с телом:
   ```json
   { "quantity": "asd" }
   ```

**Удаление по невалидному формату id:**
3. Отправить `DELETE /api/basket/{166}` (значение не соответствует формату UUID)

### Ожидаемый результат
`400 Bad Request` во всех трёх случаях с понятным сообщением о некорректном формате входных данных (например, `"quantity must be a number"` или `"invalid id format"`).

### Фактический результат
Во всех трёх случаях — `500 Internal Server Error` с сырым текстом ошибки базы данных:
```json
{ "error": "invalid input syntax for type integer: \"asf\"" }
```
```json
{ "error": "invalid input syntax for type uuid: \"{166}\"" }
```
Входные данные не проходят валидацию на уровне приложения и попадают напрямую в SQL-слой, где вызывают необработанное исключение.

---

## Сводка

| ID | Заголовок | Severity |
|---|---|---|
| BUG-001 | Отрицательный price принимается на POST и PUT | Major |
| BUG-002 | Несогласованная валидация price = 0 между POST и PUT | Major |
| BUG-003 | Вводящее в заблуждение сообщение об ошибке при price = 0 | Minor |
| BUG-004 | Категория Meat из документации не совпадает с сид-данными | Major |
| BUG-B01 | quantity = 0 возвращает "required" вместо "must be at least 1" | Minor |
| BUG-B02 | Невалидный тип quantity/id вызывает 500 с сырой ошибкой БД вместо 400 | Major |